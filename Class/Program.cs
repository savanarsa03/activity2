using System;

namespace Class
{
    class Inputdata
    {   //Deklarasi Variabel
        private double nilM, nilB;

        //method yang berfungsi untuk memasukkan data
        public void 

            //Display 'Masukkan Nilai Matematika:'
            InputData 'Nilai Matematika';

            //Accept Nilai Matematika
            nilai Matematika = Convert.ToDouble (value: Console);

            //Display "Masukkan Nilai Bahasa Inggris: "
            Inputdata 'Nilai Bahasa Inggris';

            //Accept Nilai Bahasa Inggris
            nilB Convert.ToDouble(Console.ReadLine);
    }
    class prosesdata
    {//method yang berfungsi memproses data
        //method yang melewatkan nilai
        public void

                // Compute rerata = (nilM + nilB) / 2
                rerata = ( nilM + nilB) / 2;

        //method yang mengembalikan nilai
        public double

            rerata = (nilM + nilB) / 2
            return 

        //method yang mengembalikan nilai yang memiliki parameter
        public double 
    }
    class outputdata
    {//method yang berfungsi untuk menampilkan output data
        public void

            //Display rerata
            void.outputdata rerata;

        // method menggunakan parameter
        public void

            //Display rerata
            void.outputdata
    }

    class Program
    {
        static void Main(string[] args)
        {
            //panggil method untuk input data
            //panggil method untuk proses data
            //panggil method untuk output data
        }
    }
}